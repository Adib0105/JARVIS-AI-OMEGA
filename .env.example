# JARVIS AI OMEGA V7 // RELIABLE ARC DESKTOP AGENT
# Free testing mode (default)
AI_PROVIDER=openrouter
OPENROUTER_API_KEY=put_your_openrouter_key_here
OPENROUTER_MODEL=openrouter/free
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_APP_URL=https://github.com/Adib0105/JARVIS-AI-OMEGA
OPENROUTER_APP_TITLE=JARVIS AI OMEGA V7

# Optional OpenAI mode: set AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
REASONING_EFFORT=xhigh

# V7 model routing. Blank route models reuse the primary configured model.
MODEL_ROUTING=auto
FAST_MODEL=
SMART_MODEL=
VISION_MODEL=

# Optional local OpenAI-compatible fallback. Disabled until deliberately configured.
ENABLE_LOCAL_FALLBACK=false
LOCAL_AI_BASE_URL=http://127.0.0.1:11434/v1
LOCAL_AI_MODEL=
LOCAL_AI_API_KEY=local

# Optional hybrid-memory embedding reranker.
# OFF by default. JARVIS never silently sends memory to an embedding service.
# For a local OpenAI-compatible embedding server, set BASE_URL + MODEL explicitly.
EMBEDDING_BASE_URL=
EMBEDDING_MODEL=
EMBEDDING_API_KEY=

JARVIS_NAME=JARVIS OMEGA
CREATOR_NAME=Adib Azam
USER_NAME=Adib
LANGUAGE_MODE=auto

# Web + agent tools
ENABLE_PUBLIC_WEB_TOOLS=true
ENABLE_WEB_SEARCH=true
ENABLE_CODE_INTERPRETER=true
ENABLE_LOCAL_TOOLS=true
REQUIRE_LOCAL_APPROVAL=true
ENABLE_DESKTOP_AUTOMATION=true
ENABLE_DOCUMENT_INTELLIGENCE=true
ENABLE_CODING_TOOLS=true

# V7 capability permission policies: allow | ask | always_ask | deny
PERMISSION_SYSTEM_READ=allow
PERMISSION_MEMORY_READ=allow
PERMISSION_MEMORY_WRITE=allow
PERMISSION_FILE_READ=allow
PERMISSION_FILE_WRITE=ask
PERMISSION_SCREEN_READ=ask
PERMISSION_SCREEN_CONTROL=ask
PERMISSION_BROWSER_READ=allow
PERMISSION_BROWSER_CONTROL=ask
PERMISSION_KEYBOARD_CONTROL=ask
PERMISSION_MOUSE_CONTROL=ask
PERMISSION_CODE_READ=allow
PERMISSION_CODE_WRITE=ask
PERMISSION_CODE_TEST=ask
PERMISSION_EMAIL_READ=allow
PERMISSION_EMAIL_SEND=always_ask
PERMISSION_CALENDAR_READ=allow
PERMISSION_CALENDAR_WRITE=always_ask
PERMISSION_WEB_READ=allow
PERMISSION_APP_CONTROL=ask
PERMISSION_DOCUMENT_READ=ask
PERMISSION_GIT_READ=allow
PERMISSION_ACCOUNT_CONFIG_READ=ask

# Optional Google Workspace (Gmail + Google Calendar).
ENABLE_GOOGLE_WORKSPACE=false
GOOGLE_OAUTH_CLIENT_FILE=
GOOGLE_TOKEN_FILE=

# Typed input + spoken output
ENABLE_VOICE_OUTPUT=true
VOICE_ENGINE=edge
VOICE_HINDI=hi-IN-MadhurNeural
VOICE_HINGLISH=en-IN-PrabhatNeural
VOICE_ENGLISH=en-IN-PrabhatNeural
EDGE_VOICE_RATE=-2%
EDGE_VOICE_VOLUME=+5%
EDGE_VOICE_PITCH=-20Hz
VOICE_RATE=170
VOICE_VOLUME=1.0

# Optional microphone / wake-word mode.
ENABLE_MIC_INPUT=true
ENABLE_WAKE_WORD=false
WAKE_WORD=hey jarvis
SPEECH_LANGUAGE=en-IN
MIC_RECORD_SECONDS=6

# Reliability / provider execution
AI_TIMEOUT_SECONDS=60
VISION_TIMEOUT_SECONDS=75
API_MAX_RETRIES=2
MAX_TOOL_ROUNDS=12
HISTORY_MESSAGES=36
MISSION_MAX_STEPS=5
AUTO_SUMMARIZE=false
SUMMARIZE_AFTER_MESSAGES=60

# Image upload / multimodal settings
MAX_IMAGE_ATTACHMENTS=4
MAX_IMAGE_MB=12
IMAGE_MAX_DIMENSION=1600
IMAGE_JPEG_QUALITY=82

# Dashboard / reminders
SYSTEM_REFRESH_MS=1200
REMINDER_POLL_SECONDS=5

# Structured runtime logging
JARVIS_LOG_LEVEL=INFO

# Storage
JARVIS_EXPORT_DIR=
# Optional; semicolon-separated. Blank = Desktop;Documents;Downloads;project folder
ALLOWED_FILE_ROOTS=

# V7 stores mission state and audit evidence in additive v7_* SQLite tables.
# Raw tool arguments, API keys, passwords and OAuth tokens are not persisted in the audit log.
