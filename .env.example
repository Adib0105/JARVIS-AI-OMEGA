# JARVIS AI OMEGA V6 // ARC DESKTOP AGENT
# Free testing mode (default)
AI_PROVIDER=openrouter
OPENROUTER_API_KEY=put_your_openrouter_key_here
OPENROUTER_MODEL=openrouter/free
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_APP_URL=https://github.com/Adib0105/JARVIS-AI-OMEGA
OPENROUTER_APP_TITLE=JARVIS AI OMEGA V6

# Optional paid OpenAI mode: set AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
REASONING_EFFORT=xhigh

# V6 model routing. Blank route models reuse the primary configured model.
MODEL_ROUTING=auto
FAST_MODEL=
SMART_MODEL=
VISION_MODEL=

# Optional local OpenAI-compatible fallback. Disabled until you deliberately configure a local server/model.
ENABLE_LOCAL_FALLBACK=false
LOCAL_AI_BASE_URL=http://127.0.0.1:11434/v1
LOCAL_AI_MODEL=
LOCAL_AI_API_KEY=local

JARVIS_NAME=JARVIS OMEGA
CREATOR_NAME=Adib Azam
USER_NAME=Adib
LANGUAGE_MODE=auto

# Web + agent tools
ENABLE_PUBLIC_WEB_TOOLS=true
ENABLE_WEB_SEARCH=true
ENABLE_CODE_INTERPRETER=true
ENABLE_LOCAL_TOOLS=true
REQUIRE_LOCAL_APPROVAL=true
ENABLE_DESKTOP_AUTOMATION=true
ENABLE_DOCUMENT_INTELLIGENCE=true
ENABLE_CODING_TOOLS=true

# Optional Google Workspace (Gmail + Google Calendar).
# Run setup_google.ps1 and provide your own Google OAuth Desktop client JSON first.
ENABLE_GOOGLE_WORKSPACE=false
GOOGLE_OAUTH_CLIENT_FILE=
GOOGLE_TOKEN_FILE=

# Typed input + spoken output
ENABLE_VOICE_OUTPUT=true
VOICE_ENGINE=edge
VOICE_HINDI=hi-IN-MadhurNeural
VOICE_HINGLISH=en-IN-PrabhatNeural
VOICE_ENGLISH=en-IN-PrabhatNeural
EDGE_VOICE_RATE=-2%
EDGE_VOICE_VOLUME=+5%
EDGE_VOICE_PITCH=-20Hz
VOICE_RATE=170
VOICE_VOLUME=1.0

# Optional microphone / wake-word mode. Wake listener never starts unless enabled here or switched on in the UI.
ENABLE_MIC_INPUT=true
ENABLE_WAKE_WORD=false
WAKE_WORD=hey jarvis
SPEECH_LANGUAGE=en-IN
MIC_RECORD_SECONDS=6

# Reliability / agent planning
AI_TIMEOUT_SECONDS=60
VISION_TIMEOUT_SECONDS=75
API_MAX_RETRIES=2
MAX_TOOL_ROUNDS=12
HISTORY_MESSAGES=36
MISSION_MAX_STEPS=5
AUTO_SUMMARIZE=false
SUMMARIZE_AFTER_MESSAGES=60

# Image upload / multimodal settings
MAX_IMAGE_ATTACHMENTS=4
MAX_IMAGE_MB=12
IMAGE_MAX_DIMENSION=1600
IMAGE_JPEG_QUALITY=82

# Dashboard / reminders
SYSTEM_REFRESH_MS=1200
REMINDER_POLL_SECONDS=5

# Storage
JARVIS_EXPORT_DIR=
# Optional; semicolon-separated. Blank = Desktop;Documents;Downloads;project folder
ALLOWED_FILE_ROOTS=
